import styled from "styled-components";

const ListTask = styled.ul`
  margin: 0;
  padding: 0;
  list-style: none;
`

const ListItemTask = styled.li`
  border-top: 1px solid #2f2f37;
`

export {
    ListTask,
    ListItemTask,
}